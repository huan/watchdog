{
  "name": "watchdog",
  "version": "0.7.5",
  "description": "An Timer used to Detect and Recover from Malfunctions",
  "main": "bundles/watchdog.umd.js",
  "types": "dist/watchdog.d.ts",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "tsc --module esnext",
    "clean": "shx rm -fr dist/* bundles/*",
    "dist": "npm run clean && npm run build && npm run rollup && npm run dist:es6to5",
    "dist:es6to5": "tsc --out ./bundles/watchdog.umd.js --target es5 --allowJs bundles/watchdog.es6.umd.js --lib es6,dom",
    "doc": "npm run dist && echo '# Watchdog v'$(jq -r .version package.json)' Documentation\n' > docs/index.md && jsdoc2md dist/watchdog.js >> docs/index.md",
    "demo": "ts-node examples/demo",
    "example": "npm run dmeo",
    "lint": "echo tslint v`tslint --version` && tslint --project tsconfig.json --exclude \"tests/fixtures/**\" --exclude \"dist/\" && npm run clean && tsc --noEmit",
    "rollup": "rollup -c",
    "test": "npm run lint && npm run test:unit",
    "test:unit": "blue-tape -r ts-node/register \"src/**/*.spec.ts\" \"tests/**/*.spec.ts\"",
    "test:pack": "npm run dist && export TMPDIR=/tmp/watchdog-npm.$$ && mkdir $TMPDIR && npm pack && mv watchdog-*.*.*.tgz $TMPDIR && cp tests/fixtures/smoke-testing.js $TMPDIR && cd $TMPDIR && npm init -y && npm install brolog watchdog-*.*.*.tgz && node smoke-testing.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zixia/watchdog.git"
  },
  "keywords": [
    "watchdog",
    "timer"
  ],
  "author": "Huan LI <zixia@zixia.net>",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/zixia/watchdog/issues"
  },
  "homepage": "https://github.com/zixia/watchdog#readme",
  "devDependencies": {
    "@types/blue-tape": "^0.1.31",
    "@types/node": "^9.6.6",
    "@types/semver": "^5.5.0",
    "@types/sinon": "^4.3.1",
    "blue-tape": "^1.0.0",
    "brolog": "^1.3.3",
    "jsdoc-to-markdown": "^4.0.1",
    "rollup": "^0.58.2",
    "rollup-plugin-json": "^2.3.0",
    "semver": "^5.5.0",
    "shx": "^0.2.2",
    "sinon": "^4.0.1",
    "sinon-test": "^2.1.1",
    "source-map-support": "^0.5.0",
    "ts-node": "^6.0.0",
    "tslint": "^5.7.0",
    "typescript": "^2.5.3"
  },
  "peerDependencies": {},
  "dependencies": {
    "brolog": "^1.3.3"
  },
  "files": [
    "package.json",
    "README.md",
    "CHANGELOG.md",
    "LICENSE",
    "bundles/",
    "src/",
    "dist/"
  ],
  "publishConfig": {
    "access": "public",
    "tag": "latest"
  }
}
