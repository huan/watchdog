{
  "extends": "@chatie/tsconfig",
  "compilerOptions": {
    "outDir": "dist",
  },
  "exclude": [
    "node_modules/",
    "dist/",
    "tests/fixtures/",
  ],
  "include": [
    "src/**/*.ts",
  ],
  "include_comment": "Huan(202001): we have to keep it only has one entry: src/**/*.ts so that the test:pack script will be able to run. Need to study more."
}
